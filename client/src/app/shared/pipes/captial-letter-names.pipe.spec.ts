import { CaptialLetterNamesPipe } from './captial-letter-names.pipe';

describe('CaptialLetterNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new CaptialLetterNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
